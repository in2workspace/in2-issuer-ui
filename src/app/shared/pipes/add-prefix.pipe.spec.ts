import { AddPrefixPipe } from './add-prefix.pipe';

describe('AddDetailsSuffixPipe', () => {
  it('create an instance', () => {
    const pipe = new AddPrefixPipe();
    expect(pipe).toBeTruthy();
  });
});
